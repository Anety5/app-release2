<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network for WebView / remote content -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Health Connect: declare only what you read -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SOME">

        <!-- Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Health Connect permissions rationale (internal only) -->
        <activity
            android:name=".PermissionsRationaleActivity"
            android:exported="false" />

        <!-- ✅ New Premium screen -->
        <activity
            android:name="com.somefitnessmethod.premium.PremiumTeaserActivity"
            android:exported="false"
            android:label="Premium" />
    </application>
</manifest>
