# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr


# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
android.enableJetifier=true

# Enable R8 full mode
android.enableR8.fullMode=true

# Keystore configuration (update these paths for your environment)
MYAPP_UPLOAD_STORE_FILE=C:\\Users\\Annet\\OneDrive\\Documents\\some-fitness-release.jks
MYAPP_UPLOAD_KEY_ALIAS=some-fitness-key
MYAPP_UPLOAD_STORE_PASSWORD=GunnerBoss2025#
MYAPP_UPLOAD_KEY_PASSWORD=GunnerBoss2025#
# Project-wide Gradle settings
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Build optimization
org.gradle.daemon=true

# JVM settings for Replit environment
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError

# Gradle build optimization
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Android build optimization
android.useAndroidX=true
android.enableJetifier=true
#android.enableBuildCache=true

# Kotlin build optimization
kotlin.code.style=official
kotlin.parallel.tasks.in.project=true

# R8 optimizations
android.enableR8.fullMode=true

# Build configuration
# Removed deprecated property that was causing build failures
#android.enableSeparateAnnotationProcessing=true

# Security
android.debug.obsoleteApi=false

# Performance
org.gradle.configureondemand=true
